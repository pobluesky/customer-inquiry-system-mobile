<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 둥근 모서리 반지름 -->
    <corners android:radius="8dp" />

    <!-- 배경 색상 -->
    <solid android:color="@android:color/white" />

    <!-- 테두리 설정 -->
    <stroke
        android:width="1dp"
        android:color="#03507D" />
</shape>
